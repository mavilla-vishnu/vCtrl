import { CapialiseTextDirective } from './capialise-text.directive';

describe('CapialiseTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CapialiseTextDirective();
    expect(directive).toBeTruthy();
  });
});
